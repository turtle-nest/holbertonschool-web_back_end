export default function getResponseFromAPI() {
  return new Promise((resolve, reject) => {
    // Here we could perform asynchronous processing
    // But for now, we just return an "empty" promise
    resolve();
  });
}
